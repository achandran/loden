[project]
name = "loden-theme"
version = "0.1.0"
description = "Palette audits and theme generators for Loden"
requires-python = ">=3.12"
dependencies = [
  "coloraide>=8.10,<9",
]

[dependency-groups]
dev = []

[tool.uv]
package = false
